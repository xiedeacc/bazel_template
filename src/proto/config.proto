syntax = "proto3";

package bazel_template.proto;

message BaseConfig {
  uint32 grpc_server_port = 1;
  uint32 http_server_port = 2;
  string server_addr = 3;
  uint32 metric_ratio = 4;
  uint32 metric_interval_sec = 5;
  uint32 discard_ratio = 6;
  uint32 thread_pool_size = 7;
}
